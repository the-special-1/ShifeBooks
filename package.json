{
  "name": "shifebooks-monorepo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && chmod +x ./node_modules/.bin/vite && npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}
